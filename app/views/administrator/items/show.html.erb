<h2>商品詳細</h2>
<%= image_tag @item.get_image,:size => "100x100" %>
<table class="row">
  <tbody>
    <tr>
      <th>商品名</th><td><%= @item.name %></td>
    </tr>
    <tr>
      <th>商品説明</th><td><%= @item.explanation %></td>
    </tr>
    <tr>
      <th>ジャンル</th><td><%= @item.genre_id %></td>
    </tr>
    <tr>
      <th>税込価格<br>(税抜価格)</th><td><%= @item.out_tax_price %>円</td>
    </tr>
    <tr>
        <th>販売ステータス</th>
        <td>
          <% if @item.sales_status %>
            販売中
          <% else %>
            販売中止
          <% end %>
      </td>
    </tr>
  </tbody>
</table>

<div>
  <%= link_to " 編集する",edit_administrator_item_path(@item) %>
</div>