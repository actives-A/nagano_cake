<div class="row">
  <div class ="col">
    <h2>ジャンル編集</h2>
    <%= form_with model: @genre,url: administrator_genre_path(@genre), local:true do |f| %>
    <div class="field">
      <%= f.text_field :genre_name %>
    </div>
    <div class="field">
      <%= f.submit :変更を保存, class: "btn btn-success" %>
    </div>
   <% end %>
  </div>
</div>