<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-lg-3 mt-5">
     <%= render "genre", genres: @genres %>
    </div>
    <div class="col-lg-9 mt-5">
      <div class="row">
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
          <div class="carousel-inner">
            <div class="carousel-item active">
              <%= image_tag 'cake18.png',style: "background-size: cover;", size: "850x280" ,class: "carousel_image " %>
              <div class="carousel-caption  d-md-block mb-5">
                <h4 class="card-text text-white mb-5">
                 ようこそNagano Cakeへ！<br>
                 このサイトはOnline Shopです<br>
                 会員でない方も商品の閲覧はできますが<br>
                 購入には会員登録が必要になります
                </h4>
              </div>
            </div>
            <div class="carousel-item">
              <%= image_tag 'cake16.png', size: "850x280" ,class: "carousel_image"%>
            </div>
            <div class="carousel-item">
              <%= image_tag 'cake12.png', size: "850x280" ,class: "carousel_image"%>
            </div>
          </div>
          <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
           <span class="carousel-control-prev-icon" aria-hidden="true"></span>
           <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>
        </div>
      </div>
      <h2 class="mt-5">新着商品</h2>
      <div class="row">
      <% @item.each do |item| %>
        <div class="my-auto mx-auto">
          <%= link_to item_path(item) do %>
            <%= image_tag item.get_image, size: "210x140", class: "mx-auto d-block" %>
          <% end %>
          <%= link_to item_path(item) do %>
            <p class="text-secondary mt-1 mb-0"><%= item.name %></p>
          <% end %>
          <p class="text-secondary"><%= number_to_currency(item.with_tax_price) %></p>
        </div>
      <% end %>
     </div>
     <div class="text-right">
       <%= link_to "全ての商品を見る >", items_path, class: "btn btn-outline-blue" %>
     </div>
   </div>
  </div>
</div>
